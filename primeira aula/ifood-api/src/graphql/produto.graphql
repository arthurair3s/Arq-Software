type Produto {
  id: ID!
  nome: String!
  descricao: String
  preco: Float!
}

extend type Query {
  produtos: [Produto]
  produto(id: ID!): Produto
}

extend type Mutation {
  criarProduto(
    nome: String!
    descricao: String
    preco: Float!
  ): Produto

  editarProduto(
    id: ID!
    nome: String!
    descricao: String
    preco: Float!
  ): Produto

  deletarProduto(id: ID!): Boolean
}
